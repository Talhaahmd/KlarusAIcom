export const metadata = {
    title: 'Klarus AI',
    description: 'Klarus AI Application',
  };
  
  export default function RootLayout({ children }) {
    return (
      <html lang="en">
        <body>{children}</body>
      </html>
    );
  }
  